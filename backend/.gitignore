node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Optimized site images (gallery, testimonials) are tracked in git so they
# survive fresh clones and deploys — the raw multi-MB originals are never committed.
